/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/ai-order-demo',
  assetPrefix: '/ai-order-demo',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
